{
  "name": "@nycopportunity/pattern-typography",
  "version": "1.0.2",
  "description": "This project was scaffolded using the NYCO Patterns CLI",
  "scripts": {
    "version": "git add .",
    "prepublishOnly": "git push && git push --tags"
  },
  "devDependencies": {
    "@nycopportunity/pttrn": "latest"
  }
}
